#include <stdio.h>

 int op_add(int a, int b)
 {
    return (a + b);
 }

 int op_sub(int a, int b)
 {
    return (a  - b);
 }

 int op_mul(int a, int b)
 {
    return (a * b);
 }

 int op_div(int a, int b)
 {
    /* ! some checks here for valid operands */
    return (a / b);
 }

 int op_mod(int a, int b)
 {
    /* ! some check here for valid operands */
    return (a % b);
 }
